#include "main.h"

/**
 * set_string - sets a pointer to a character
 * @s: the address of the pointer to set
 * @to: the pointer to the character
 *
 * Return: always returns void
 */

void set_string(char **s, char *to)
{
	*s = to;
}
